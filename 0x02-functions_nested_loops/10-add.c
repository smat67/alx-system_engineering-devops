#include "main.h"
#include <stdio.h>
/**
 * add - Adds two numbers.
 * @num1: the first number to be added
 * @num2: the second number to be added.
 *
 * Return: The results after adding numbers
 */
int add(int inum1, int num2)
{
	return (num1 + num2);
}
